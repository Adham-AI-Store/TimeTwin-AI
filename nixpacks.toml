[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python -m venv .venv",
  ". .venv/bin/activate && pip install -r backend/requirements.txt"
]

[start]
cmd = ". .venv/bin/activate && cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"